redmine_port: 3000

db_host: rc1d-xxxxxxxx.mdb.yandexcloud.net
db_name: redmine
db_user: redmine
db_password: "{{ vault_db_password }}"
datadog_api_key: "{{ vault_datadog_api_key }}"

datadog_site: "datadoghq.com"

datadog_checks:
  http_check:
    init_config:

    instances:
      - name: redmine
        url: "http://localhost:{{ redmine_port }}"
